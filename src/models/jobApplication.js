export default class JobApplication {
  constructor(
    id,
    statusApplication,
    userUsername,
    skills,
    email,
    description,
    score
  ) {
    this.id = id;
    this.status = status;
    this.userUsername = userUsername;
    this.skills = skills;
    this.email = email;
    this.description = description;
    this.score = score;
  }
}
